# Configuración de Base de Datos
DATABASE_URL=mysql+mysqlconnector://root:password@localhost/gestion_plantas

# Clave secreta (generar con: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=tu_clave_secreta_aqui_64_caracteres

# Configuración de Email (para alertas)
MAIL_USERNAME=tu_correo@gmail.com
MAIL_PASSWORD=tu_password_de_app

# Google Drive API (opcional)
GOOGLE_DRIVE_FOLDER_ID=tu_folder_id_de_drive
GOOGLE_CREDENTIALS_FILE=credentials.json

# Entorno
FLASK_CONFIG=development